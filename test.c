#include <stdio.h>
#include <string.h>


char ack_topic[20]={0};
strcat(ack_topic,"gateway_sub1");


int main()
{
        char ack_payload[50]={0};
        strcat(ack_payload,"msg received from topic : ");
        strcat(
                
        );
        printf("%s",ack_payload);


        return 0;
}